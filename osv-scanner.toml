[[IgnoredVulns]]
id = "GHSA-73rr-hh4g-fpgx"
reason = "diff@5.2.0 is a transitive dependency via @oclif/plugin-plugins > npm > libnpmdiff. rdme does not use diff's parsePatch/applyPatch functions; the vulnerable code path is never exercised."

[[IgnoredVulns]]
id = "GHSA-fj3w-jwp8-x2g3"
reason = "fast-xml-parser@5.3.6 is a transitive dev-only dependency via oclif > @aws-sdk/client-cloudfront. Not included in production builds; rdme does not use XML parsing or building."

[[IgnoredVulns]]
id = "GHSA-5j98-mcp5-4vw2"
reason = "glob@10.4.5 is a transitive dependency via @oclif/plugin-plugins > npm. Vulnerability is in glob's CLI (-c/--cmd flag) only, not the library API. rdme never invokes the glob CLI."

[[IgnoredVulns]]
id = "GHSA-23c5-xmqv-rm74"
reason = "minimatch@9.0.5 is a transitive dependency via @oclif/plugin-plugins > npm. rdme does not pass user-controlled glob patterns to minimatch; only used internally by npm's dependency resolution."

[[IgnoredVulns]]
id = "GHSA-3ppc-4f35-3m26"
reason = "minimatch@9.0.5 is a transitive dependency via @oclif/plugin-plugins > npm. rdme does not pass user-controlled glob patterns to minimatch; only used internally by npm's dependency resolution."

[[IgnoredVulns]]
id = "GHSA-7r86-cg39-jmmj"
reason = "minimatch@9.0.5 is a transitive dependency via @oclif/plugin-plugins > npm. rdme does not pass user-controlled glob patterns to minimatch; only used internally by npm's dependency resolution."

[[IgnoredVulns]]
id = "GHSA-34x7-hfp2-rc4v"
reason = "tar@6.2.1/7.4.3 is a transitive dependency via @oclif/plugin-plugins > npm. rdme never extracts tar archives; its archive handling uses the unzipper package for zip files only."

[[IgnoredVulns]]
id = "GHSA-83g3-92jg-28cx"
reason = "tar@6.2.1/7.4.3 is a transitive dependency via @oclif/plugin-plugins > npm. rdme never extracts tar archives; its archive handling uses the unzipper package for zip files only."

[[IgnoredVulns]]
id = "GHSA-8qq5-rm4j-mr97"
reason = "tar@6.2.1/7.4.3 is a transitive dependency via @oclif/plugin-plugins > npm. rdme never extracts tar archives; its archive handling uses the unzipper package for zip files only."

[[IgnoredVulns]]
id = "GHSA-r6q2-hw4h-h46w"
reason = "tar@6.2.1/7.4.3 is a transitive dependency via @oclif/plugin-plugins > npm. rdme never extracts tar archives; its archive handling uses the unzipper package for zip files only."
